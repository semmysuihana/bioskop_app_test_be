<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Movie</title>
</head>
<body>

  <h1>Edit Movie</h1>

  <form action="/edit-movies/<%= data.id %>" method="POST">
    
    <p>
      <label for="title">Judul:</label><br>
      <input type="text" id="title" name="title" value="<%= data.title %>" required>
    </p>

    <p>
      <label for="description">Deskripsi:</label><br>
      <textarea id="description" name="description" rows="4" cols="50" required><%= data.description %></textarea>
    </p>

    <p>
      <label for="duration_minutes">Durasi (menit):</label><br>
      <input type="number" id="duration_minutes" name="duration_minutes" value="<%= data.duration_minutes %>" required>
    </p>

    <p>
      <button type="submit">Simpan Perubahan</button>
    </p>

  </form>

</body>
</html>
